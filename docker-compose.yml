services:
  web:
    image: beatcell-white:latest
    restart: always
    ports:
      - "8009:80"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost/"]
      interval: 30s
      timeout: 5s
      retries: 3
